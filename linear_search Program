def linear_search(arr, tgt):
    found = False
    for i in range(len(arr)):
        if arr[i] == tgt:
            print("Element found at position:", i)
            found = True
            break
    if not found:
        print("Element not found")

arr = [10, 20, 30, 40, 50]
tgt = int(input("Enter element to search: "))
linear_search(arr, tgt)
